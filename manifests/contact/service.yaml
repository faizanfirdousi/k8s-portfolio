# manifests/contact/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: contact
  namespace: contact
spec:
  selector:
    app: contact
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
